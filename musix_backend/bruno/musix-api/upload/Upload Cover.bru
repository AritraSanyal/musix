meta {
  name: Upload Cover
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/upload/cover
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8080
}
