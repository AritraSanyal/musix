meta {
  name: Upload Audio
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/upload/audio
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:8080
}
